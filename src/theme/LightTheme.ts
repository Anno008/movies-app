import * as StyledComponents from "styled-components";

const LightTheme: StyledComponents.DefaultTheme = {
  primaryTextColor: "#131414",
  primaryBackgroundColor: "#666",
  secondaryBackgroundColor: "#999",
  headerBackgroundColor: "#888",
  textShadow: "#FFF"
};

export { LightTheme };
